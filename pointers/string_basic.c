#include <stdio.h>

int main() {
    // Declaration and initialization of a string
    char str1[] = "Hello, world!";

    // Declaration and initialization of a string with explicit size
    char str2[10] = "Hello";

    // Print the strings
    printf("String 1: %s\n", str1);
    printf("String 2: %s\n", str2)

    return 0;
}