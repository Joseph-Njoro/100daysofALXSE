#include <stdio.h>

int add(int, int)

int main(){
    int a=90, int b=78, int c=102, int d;
    d=add(a,b,c);
    printf("Summation of a b & c is d. d is: %d",d);
}
{
    int add(int a,int b,int c);
    int z=(a+b+c);
    return z;
}